<p style="color: green"><%= notice %></p>

<h1>Friends</h1>
<table class = "table table-striped table-hover table-borderless">
<thead calss = "">
<tr class="table-secondary">
<th>First name</th>
<th>Last name</th>
<th>Email</th>
<th>Phone</th>
<th>Twitter</th>
<th colspan = "3"></th>
</tr>
</thead>

<tbody>
<%# <div id="friends"> %>

  <% @friends.each do |friend| %>
    <%# <%= render friend %>
    <tr>
    <td><%= friend.first_name%></td>
    <td><%= friend.last_name%></td>
    <td><%= friend.email%></td>
    <td><%= friend.phone%></td>
    <td><%= friend.twitter%></td>
    <td><%= link_to "show", friend, class: "btn btn-info"%></td>
    <td><%= link_to "edit", edit_friend_path(friend), class: "btn btn-secondary"%></td>
    <td><%= button_to friend_path(friend), class: "btn btn-danger", method: :delete, confirm: "Are you sure?" do%>Delete <% end %> </td>
    </tr>
    <%# <p>
      <%= link_to "Show this friend", friend %>
    <%# </p> %>
  <% end %>
<%# </div> %>
</tbody>
</table>
<%= link_to "New friend", new_friend_path, class: "btn btn-outline-primary" %>
